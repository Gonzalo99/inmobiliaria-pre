export class Imagenes {

    public id: number;
    public code: number;
    public ruta: string;

    constructor(id: number, code: number, ruta: string) {

        this.id = id;
        this.code = code;
        this.ruta = ruta;

    }

}